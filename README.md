**This is the sha256 generator excercise**

**Usage**
After "mvn clean package" you will finde the shaded jar in target folder.
Just execute the jar with `java -jar filename-shaded.jar source.file destination.file`

